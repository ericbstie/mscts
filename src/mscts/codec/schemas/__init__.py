"""Packet schemas of Target 26.3, one module per connection state.

Each module maps packet names to schemas in `SERVERBOUND` and `CLIENTBOUND`,
and records the minecraft.wiki revision its field layouts were taken from.
"""
